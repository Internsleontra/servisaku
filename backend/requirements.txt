# FastAPI & server
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.13.2

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# Validation & serialization
pydantic[email-validator]>=2.9.2
pydantic-settings>=2.5.2

# Supabase Storage (optional, for file uploads)
supabase>=2.7.2

# Payments (Billplz via httpx; iPay88 stub — no HTTP client needed until credentials exist)
httpx>=0.28.0

# Media uploads (Cloudinary free tier)
cloudinary>=1.40.0,<2.0.0

# Notifications — push (Firebase Admin SDK); email providers use httpx above
firebase-admin>=7.0.0,<8.0.0

# Real-time (Socket.IO over ASGI)
python-socketio>=5.11.0,<6.0.0

# Security — rate limiting (in-memory by default; Redis-ready via
# RATE_LIMIT_STORAGE_URI, see .env.example)
slowapi>=0.1.9

# Logging
structlog>=24.4.0

# Env
python-dotenv>=1.0.1

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# Reporting (scripts/generate_final_report.py — not needed at app runtime)
python-docx>=1.1.0
reportlab>=4.2.0
