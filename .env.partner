VITE_APP=partner
VITE_APP_TITLE=ServisAku Partner

# Backend mode: real | mock | auto (default auto — probes /api/health once).
# Pin to "real" when running the Express server, or "mock" for pure demo, to
# avoid any runtime auto-switching. Override locally in .env if needed.
# VITE_BACKEND=auto
